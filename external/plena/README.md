# PLENA C++ source

`src/plena.cpp` is included unchanged. Build and integration instructions are
in [docs/PLENA.md](../../docs/PLENA.md). `UPSTREAM.json` identifies the exact
source revision and file checksums. The original LICENSE, PROVENANCE.md and
CITATION.cff are retained unchanged alongside the source.
